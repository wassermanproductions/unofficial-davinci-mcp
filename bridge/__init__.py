"""In-Resolve live bridge for the free edition.

The runnable script is :mod:`bridge.resolve_bridge`; it is copied into DaVinci
Resolve's Scripts folder and launched from inside the app. This package marker
only exists so the script can be imported by the test-suite and the installer -
end users copy the single ``resolve_bridge.py`` file, nothing else.
"""
